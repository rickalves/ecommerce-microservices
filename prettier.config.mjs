/**
 * @see https://prettier.io/docs/configuration
 * @type {import("prettier").Config}
 */
const config = {
    trailingComma: 'es5',
    tabWidth: 4,
    singleQuote: true,
    printWidth: 100,
    useTabs: false,
    semi: true,
    bracketSpacing: true,
    arrowParens: 'always',
};

export default config;
